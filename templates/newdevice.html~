{% extends "layout.html" %}

{% block header %}
{{super()}}
{% endblock %}

{% block content %}

  

<form id="newDevice" action="{{url_for('newDevice')}}" method="POST"> 
	Host Name: <input type="text" name="hostName" /></br> 
	IP Address: <input type="text" name="ipAddress" /></br>
	API Username: <input type="text" name="apiUserName" /> </br>
	API Password: <input type="text" name="apiPassword" /> </br>
	Details:</br> <textarea name="details"></textarea> </br>

    <input type="submit" value="Submit New F5 Device" /> 
</form>


{% endblock %}
 
{% block scripts %}

<!--<script src="http://malsup.github.com/jquery.form.js"></script> 
 
    <script> 
        // wait for the DOM to be loaded 
        $(document).ready(function() { 
            // bind 'myForm' and provide a simple callback function 
			$('#newDevice').ajaxForm({ beforeSubmit: validate}); 
        });
	



	function validate(formData, jqForm, options) {
		var hostNameValue = $('input[name=hostName]').fieldValue();
		var ipAddressValue = $('input[name=ipAddress]').fieldValue();
		var apiUserNameValue = $('input[name=apiUserName]').fieldValue();
		var apiPasswordValue = $('input[name=apiPassword]').fieldValue();
	
		if (!hostNameValue[0]) {
			alert('You must enter a value for the Host Name');
			return false;
		}
		if (!ipAddressValue[0]) {
			alert('You must enter a value for the ipAddress');
			return false;
		}
		if (!apiUserNameValue[0]) {
			alert('You must enter a value for the API Username');
			return false;
		}
		if (!apiPasswordValue[0]) {
			alert('You must enter a value for the API Password');
			return false;
		}
	}

    </script>--!> 

{% endblock %}
