{% extends "layout.html" %}
{% block header %}
{{super()}}
{% endblock %}
{% block content %}
<div class="row">
	<div class="col-sm-4"><h2>{{device.hostName}}</h2></div>
</div>
<!--<ul>--!>
	<div class="list-group">
		<div class="list-group-item"><h4>Ip Address: </h4>
			<p>{{device.ipAddress}}</p>
		</div>
		<div class="list-group-item"><h4>Description: </h4>
			<p>{{device.details}}</p>
		</div>
		{% if device.apiUserName %}
		<div class="list-group-item"><h4><font color=green>API Username is set</font></h4>
		</div>
		{% else %}
		<div class="list-group-item-danger"><h4>API Username is not set</h4>
			<p>The service may be unable to access API information from this device.</p>
		</div>
		{% endif %}
		{% if device.apiPassword %}
		<div class="list-group-item"><h4><font color=green>API Password is set</font></h4>
		</div>
		{% else %}
		<div class="list-group-item-danger"><h4>API Username is not set</h4>
			<p>The service may be unable to access API information from this device.</p>
		</div>
		{% endif %}	
	</div>
	<!--<li>Description: {{device.details}}</li>
	{% if device.apiUserName %}
	<li><font color=green>API Username is set</font></li>
	{% else %}
	<li><font color=red>API Username is not set</font></li>
	{% endif %}
	{% if device.apiPassword %}
	<li><font color=green>API Password is set</font></li>
	{% else %}
	<li><font color=red>API Password is not set</font></li>
	{% endif %}--!>
</ul>

<a href="{{url_for('newStat', device_id=device.id)}}">Add New Stat</a>
{% endblock %}
